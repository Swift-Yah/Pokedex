query Trainer($name: String!) {
    Trainer(name: $name) {
        name
        ownedPokemons {
            name
            url
        }
        _ownedPokemonsMeta {
            count
        }
    }
}
